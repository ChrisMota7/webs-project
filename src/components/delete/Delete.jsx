import React from 'react'
import GenericTable from '../GenericTable/GenericTable'

const Delete = (props) => {
    const columns = [
        "column 1",
        "column 2",
        "column 3"
    ];

    <GenericTable numColums={columns}/>
}

export default Delete;